var transportation = ['car', 'bike', 'auto', 'train', 'one of my favourite skuttie'];
for (var i = 0; i < transportation.length; i++) {
    console.log('I would like to own a ' + transportation[i]);
}